# ============================================
# Sora Auto Toy 配置文件示例
# 复制此文件为 .env 并根据需要修改配置
# ============================================

# --------------------------------------------
# 账户配置
# --------------------------------------------

# 默认密码 (用于所有注册的账户)
# 要求: 至少8位，包含大小写字母、数字和特殊字符
DEFAULT_PASSWORD=YourSecurePassword123!

# --------------------------------------------
# 浏览器配置
# --------------------------------------------

# 是否使用无头模式 (true/false)
# true: 不显示浏览器窗口，后台运行
# false: 显示浏览器窗口，可以看到操作过程
HEADLESS=false

# 操作延迟 (毫秒)
# 每个操作之间的延迟时间，值越大操作越慢
# 建议: 50-100，网络慢时可以适当增加
SLOW_MO=50

# Chrome 浏览器路径 (可选)
# 如果不设置，将使用 Puppeteer 自带的 Chromium
# Windows 示例: C:\Program Files\Google\Chrome\Application\chrome.exe
# macOS 示例: /Applications/Google Chrome.app/Contents/MacOS/Google Chrome
# Linux 示例: /usr/bin/google-chrome
CHROME_PATH=

# --------------------------------------------
# 超时配置 (毫秒)
# --------------------------------------------

# 页面导航超时时间
# 页面加载的最大等待时间
NAVIGATION_TIMEOUT=60000

# 等待邮件验证码超时时间
# 等待验证码邮件到达的最大时间
WAIT_FOR_EMAIL_TIMEOUT=120000

# --------------------------------------------
# 代理配置 (可选)
# --------------------------------------------

# HTTP 代理地址
# 格式: http://host:port 或 http://user:pass@host:port
# HTTP_PROXY=http://127.0.0.1:7890

# SOCKS5 代理地址
# 格式: socks5://host:port 或 socks5://user:pass@host:port
# SOCKS_PROXY=socks5://127.0.0.1:1080

# --------------------------------------------
# 调试配置
# --------------------------------------------

# 截图保存目录
SCREENSHOT_DIR=screenshots

# 是否保存调试截图 (true/false)
SAVE_DEBUG_SCREENSHOTS=true
